# Assignment

You were given two new files that would land in the same feature together. **Treat them as a code review** the way you would a pull request: look for problems in correctness, API/data handling, React behavior, performance where it matters, clarity, and consistency.

You can assume React and TypeScript are the stack. There is no other repository context; review only what is in these files and what they would reasonably imply for callers.
